# FABLE 5 — environment variables
# The local-first MVP needs none of these. They exist as a map for the future
# Supabase-backed deployment.

# Path to the local SQLite file used by src/lib/db.ts (defaults to db/fable5.db)
DATABASE_FILE=db/fable5.db

# --- Supabase (future) -------------------------------------------------
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
